---
name: project_overnight_audit_fix_sequence
description: "Atlas 2026-05-30 overnight audit — 47 findings, locked 9-chunk fix sequence, and the 3 approved decisions for execution"
metadata: 
  node_type: memory
  type: project
  originSessionId: c880c6b8-5d45-4717-8ecc-d02304a45cd0
---

2026-05-30 overnight audit produced `docs/v6/2026-05-30-overnight-audit.md` (47 findings: 1 CRIT, 9 HIGH, 19 MED, 18 LOW; 3 refuted). Root cause of all data staleness: `mv_refresh_v6_all` (migration 111) omits 7 MVs from migration 098, so landing/stocks/markets-rs froze while source tables stayed fresh.

**Done that night (operational, committed at 3c12a444):** refreshed 11 fresh-source MVs → landing page + /markets-rs + sector pages now at Friday 05-29. Reclassified `mv_stock_list_v6` from "7d stale" to "date-LABEL bug" — its row data is fresh (verified via RELIANCE composite), but `as_of_date` reads the dead legacy table `atlas_conviction_daily` (frozen 05-22) instead of live `atlas_stock_conviction_daily`. `mv_stock_landscape` stuck uniformly at 05-27 — definition anchor bug.

**Approved execution decisions (2026-05-30):**
1. **Session 1 = "data layer" (Chunk A+):** migration 121 to add the 6 missing MVs to the `mv_refresh_v6_all` cron + fix the 2 MV date-anchor bugs (`mv_stock_list_v6` → repoint as_of_date to `atlas_stock_conviction_daily`; `mv_stock_landscape` → find why anchor returns 05-27) + re-run the stale writer chain for `atlas_signal_calls` (needs scorecard→regime→signals order) and `atlas_etf_scorecard`. Apply migration via **alembic on EC2** (psycopg2 works there), NOT MCP out-of-band (the out-of-band DDL pattern caused the whole 098/120 mess — see [[feedback_precommit_hooks]] / deploy hygiene guide).

**✅ Chunk C DONE 2026-05-30** (PR #94, main `b20d7837`; audit doc §10). Funds + sectors were NOT a build — the fund generator (`atlas/inference/fund_scorecard.py` `compute_fund_scorecard_from_engine`+`emit_upsert_sql`, 4-layer methodology in `docs/v6/fund-etf-ranking-methodology.md`) and the 8 sector v6 compute fns (written by `scripts/sector_5y_backfill.py`) already existed; just never wired into nightly. Funds: new direct-write wrapper `scripts/ops/run_fund_scorecard_for_date.py` (marker-free, mirrors ETF) → 587 rows @ 05-29, `nav_as_of`=05-27 (AMFI weekend NAV lag, de_mf_nav MAX=05-27). Sectors: `sector_5y_backfill.py --start-year 2026` → 30/30 @ 05-29. Wired 4 NON-FATAL steps [5e]-[5h] into `run_atlas_nightly.sh` (stock-RS, sector v6, fund+ETF scorecard) after M2-M5, before the 21:45 UTC MV cron; deployed to EC2 (next run Mon; cron Mon-Fri). **ALL 13 v6 surfaces now @ 05-29 and self-refreshing.** Follow-ups (noted, not done): fund/ETF scorecard conviction layer still reads dead `atlas_conviction_daily` (layer 2, 25% wt); year-recompute of sectors nightly could be a lighter daily step. Remaining audit work = B (frontend coherence) / D (dead code) / E (API hygiene) / F (API breaking) / G (tests) / H (refactors) — all non-data polish.

**✅ Chunk A+ DONE 2026-05-30 morning** (branch `fix/v6-chunk-a-data-layer`; alembic head=121 on EC2; audit doc §9 has the full log). Migration 121 = cron re-registered with **all 16** v6 MVs (not 6/7 — enumerated the full set; was 9) + `mv_stock_list_v6` `latest` CTE repointed to live `atlas_stock_conviction_daily`. Writer chain to 05-29: `python -m atlas.inference.cli --target-date 2026-05-29` (the daily_inference orchestrator — writes scorecard_daily+regime_daily+signal_calls in ONE run; was the "last ran 05-27" gap), `compute_conviction --as-of … --persist`, plus two NEW durable ops scripts: `scripts/ops/backfill_stock_rs_nifty500.py` and `scripts/ops/run_etf_scorecard_for_date.py`. KEY finding: **`mv_stock_landscape` was NOT a definition bug** — its anchor `MAX(date) WHERE rs_3m_nifty500 IS NOT NULL` is correct; the `rs_*_nifty500` cols have NO daily writer (only `rs_*_tier` are written by M2) so they were NULL for 05-29. Formula `rs_X = ret_X − NIFTY500.ret_X` verified to 0.0 residual on 05-27 (4 CA-stock exceptions). Verified GREEN: landing/markets-rs/stocks/stock-detail/sectors/etfs all 05-29. **STILL 05-22 → Chunk C (NOT regressions):** `mv_fund_list_v6`/`mv_fund_deepdive` (no fund scorecard generator) + `mv_sector_deepdive` (sector v6 cols not wired — finding #9). RECURRING RISK: stock rs + sector v6 cols re-freeze nightly until the backfills are wired into `run_atlas_nightly.sh` (Chunk C).
2. **Fund scorecard = BUILD FRESH** (Chunk C): `atlas_fund_scorecard` has NO generator — nothing ever wrote it on a schedule (frozen 05-22). Build the writer from methodology + wire into `run_atlas_nightly.sh`. NOT part of session 1.
3. **Session-rotation hook = deferred** (user reviewing `docs/v6/2026-05-30-session-rotation-hook.md` first; do not auto-install into settings.json).

**Chunk order:** A+ (data layer) → B (frontend coherence: clickable sectors, empty-state context, "why Cautious" driver clause, tooltips) → C (fund scorecard build + sector v6 columns) → D (dead code) → E (API hygiene non-breaking) → F (API breaking: URL versioning, cursor pagination, session-token auth) → G (backend tests) → H (refactors).

**Do NOT touch (intentional, refuted findings):** Cautious-regime-with-green-tiles is coherent by design (tiles = single signal family, regime = compound VIX/200-EMA gating); the fix is explanation (Chunk B), not threshold alignment. `MultiTenureReturnsTable` self-ref ticker stays plain text. pg_cron scheduler is NOT dead. See report §6.

Gated paths (migrations/**, atlas/compute/**, frontend/src/**) require a planning skill (/tdd or /plan-eng-review) invoked first in-session. Links: [[project_v6_2026_05_26_overnight_state]], [[reference_ec2_access]], [[feedback_backend_100_before_frontend_mockups_strict]].
