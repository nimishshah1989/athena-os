---
name: feedback_auto_context
description: When context runs low, auto-save memory and restart — never ask user for permission to continue
type: feedback
---

When approaching context limits, save all WIP state to memory files automatically and continue working in a new session. Do not pause to ask the user — just save and restart. The user expects autonomous operation.
